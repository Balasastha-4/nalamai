# NalaMAI AI Service Environment Variables

# Environment
ENVIRONMENT=development
DEBUG=False

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Google Generative AI Configuration
GOOGLE_API_KEY=your_google_ai_studio_key
# If 2.0 free tier shows "limit: 0", use gemini-1.5-flash or gemini-pro.
GEMINI_MODEL=gemini-1.5-flash
# Skip Gemini (no 429 noise); agent uses real backend tools only.
# AGENT_TOOLS_ONLY=true

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# Backend API Configuration
BACKEND_API_URL=http://localhost:8080
BACKEND_API_KEY=your_backend_api_key_here

# ML Model Configuration
RISK_PREDICTION_MODEL=random_forest
CONFIDENCE_THRESHOLD=0.6

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# Database Configuration (if applicable)
DATABASE_URL=postgresql://user:password@localhost:5432/nalamai

# OCR Configuration
OCR_PROVIDER=google_vision
OCR_API_KEY=your_ocr_api_key_here

# JWT Configuration
SECRET_KEY=your_secret_key_for_jwt_signing_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
